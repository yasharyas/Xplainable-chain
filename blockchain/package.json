{
  "name": "xai-chain-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for XAI-Chain blockchain security platform",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network mumbai",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node"
  },
  "keywords": ["blockchain", "ethereum", "solidity", "xai", "security"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.10.0"
  }
}
